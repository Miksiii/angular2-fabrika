import { SubsetPipePipe } from './subset-pipe.pipe';

describe('SubsetPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SubsetPipePipe();
    expect(pipe).toBeTruthy();
  });
});
